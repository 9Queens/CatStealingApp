{
    "Docker": {
        "ComposeFile": "../../../../../docker-compose.yml", //we assume the file will awlays be in the root !!!!!  (always one level above our test project !!! )
        "ApiBaseUrl": "http://localhost:5000",
        "ConnectionAttempts": 60, // how many times NOT seconds
        "DelayInSeconds": 2, // seconds

        "WaitBeforeTearDownInSeconds": 3, // seconds
        "WaitBeforeTearUp": 3 // seconds

    }
}